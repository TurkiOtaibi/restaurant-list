# PostgreSQL

Sprint 0 uses the root `docker-compose.yml` PostgreSQL service for local development.

No schema objects are created in Sprint 0. Future schema changes must be added through Alembic migrations under `backend/migrations`.
