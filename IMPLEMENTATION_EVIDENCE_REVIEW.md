# Implementation Evidence Review

## Scope

This review covers only features classified in `IMPLEMENTATION_COVERAGE_REPORT.md` as:

- Partially Implemented
- Not Implemented
- Unable to Verify

Features already classified as Implemented are intentionally excluded.

Evidence standard: actual implementation files only. Test-case files and automated test files are not used as implementation evidence.

## Summary

| Classification Reviewed | Count |
|---|---:|
| Partially Implemented | 16 |
| Not Implemented | 9 |
| Unable to Verify | 4 |
| Total Reviewed | 29 |

## Feature Evidence Review

| Feature | Classification Reason | Implementation Evidence Searched | Source Files Searched | Classes / Components / Endpoints Inspected | Why Evidence Is Insufficient | May Exist Under Another Name | Confidence | Recommended Next Action |
|---|---|---|---|---|---|---|---|---|
| LIST-007 - View owned list detail | Detail API and detail UI exist, but large-list virtualization/responsive handling from the approved stories was not found. | Owned list detail endpoint, list detail service, list detail page, list item rendering, frontend list-detail page structure. | `backend/app/api/lists.py`; `backend/app/modules/lists/services.py`; `backend/app/modules/lists/schemas.py`; `frontend/app/lists/[id]/page.tsx`; `frontend/src/components/ui/ListCard.tsx`; `frontend/app/globals.css`. | `GET /api/v1/lists/{list_id}`; `list_detail_response`; `get_owned_user_list`; `/lists/[id]` page component. | Existing evidence proves list detail retrieval/rendering, but not virtualization, bounded DOM strategy, or documented large-list responsiveness. | Possible under generic CSS/layout, but no named virtualization or large-list component was found. | High | Keep as Partial |
| LIST-008 - Search and add existing place | Search and add are implemented, but no evidence was found for a prefilled create-place flow from a failed search/no-result state. | Add-place dialog search, places search API, add-to-list API, no-result UI, create-place navigation. | `frontend/src/features/lists/AddPlaceDialog.tsx`; `frontend/app/places/page.tsx`; `frontend/src/features/places/CreatePlaceDialog.tsx`; `backend/app/api/places.py`; `backend/app/api/lists.py`; `backend/app/modules/places/services.py`; `backend/app/modules/lists/services.py`. | `AddPlaceDialog`; `GET /api/v1/places`; `POST /api/v1/lists/{list_id}/items`; `list_place_summaries`; `add_place_to_list`. | Existing evidence proves search and add. It does not prove that the original search term is carried into place creation. | Could be implicit in URL/query handling, but no implementation evidence of query-to-create prefill was found. | High | Keep as Partial |
| LIST-010 - Remove place from owned list | Remove API and UI action exist, but documented undo/restore behavior was not found. | Remove item endpoint, list detail remove UI, service delete behavior, restore/undo keyword search. | `backend/app/api/lists.py`; `backend/app/modules/lists/services.py`; `frontend/app/lists/[id]/page.tsx`; `frontend/src/features/lists/AddPlaceDialog.tsx`; `frontend/src/lib/api.ts`. | `DELETE /api/v1/lists/{list_id}/items/{place_id}`; `delete_place_from_owned_list`; remove button/action in `/lists/[id]`. | Existing evidence proves permanent removal. It does not prove undo, expiry window, restore endpoint, or client-side undo state. | Could exist as a generic toast undo component, but no restore action or undo state was found in the inspected list flow. | High | Keep as Partial |
| PLACE-001 - View places list | Places list API and UI exist, but explicit UI pagination/infinite scrolling behavior was not found. | Places collection API, places service pagination, places page, place card list rendering, query state. | `backend/app/api/places.py`; `backend/app/modules/places/services.py`; `backend/app/modules/places/schemas.py`; `frontend/app/places/page.tsx`; `frontend/src/features/places/PlaceLibraryPage.tsx`; `frontend/src/components/ui/PlaceCard.tsx`. | `GET /api/v1/places`; `list_place_summaries`; `PlaceLibraryPage`; `PlaceCard`. | Backend supports `limit`/`offset`, but UI evidence does not show page controls, infinite scroll, next-page loading, or documented continuous browsing behavior. | Could be hidden in a shared pagination component, but no such component was found in the places page path. | High | Keep as Partial |
| PLACE-007 - Highest average rating first, unrated last | Rating sort is implemented, but documented paging/continuous list behavior tied to the sorted list was not found. | Places sorting query, SQL ordering by average rating, UI sort parameter, place list rendering, pagination controls. | `backend/app/api/places.py`; `backend/app/modules/places/services.py`; `frontend/src/features/places/PlaceLibraryPage.tsx`; `frontend/src/components/ui/PlaceCard.tsx`; `frontend/src/lib/api.ts`. | `GET /api/v1/places?sort=rating_desc`; `list_place_summaries`; SQL `avg(Rating.rating).desc().nulls_last()` ordering; `PlaceLibraryPage`. | Existing evidence proves sorting. It does not prove sorted pagination/continuous-scroll behavior on the UI. | Could be covered by backend-only pagination, but no UI continuation behavior was found. | High | Keep as Partial |
| RATING-005 - Tried derived from rating row | Tried state is derived correctly on API responses, but no evidence was found for live cross-tab synchronization. | Rating create/update service, place summary derived fields, profile tried counts, frontend refresh paths, browser synchronization mechanisms. | `backend/app/modules/ratings/services.py`; `backend/app/modules/places/services.py`; `backend/app/modules/profile/services.py`; `frontend/app/places/[id]/page.tsx`; `frontend/app/places/[id]/rate/page.tsx`; `frontend/src/lib/api.ts`. | `create_or_update_rating`; `current_user_tried`; profile tried counts; `BroadcastChannel` in auth API client. | Existing evidence proves derived state after API refresh. It does not prove deterministic multi-tab propagation or live synchronization after rating changes. | Could exist as incidental page revalidation, but no rating-specific cross-tab event or subscription was found. | High | Keep as Partial |
| RESP-003 - 200% zoom/adaptive pressure | Responsive CSS exists, but static code evidence cannot prove 200% zoom behavior across documented screens. | Global CSS responsive rules, viewport meta, layout constraints, dialog/list/card layouts, touch-target sizing. | `frontend/app/globals.css`; `frontend/app/layout.tsx`; `frontend/src/components/AppNav.tsx`; `frontend/src/components/ui/Dialog.tsx`; `frontend/src/components/ui/PlaceCard.tsx`; `frontend/src/components/ui/ListCard.tsx`. | CSS media queries; `minmax(0, 1fr)` grid usage; safe-area variables; focus/touch target styles. | CSS suggests adaptive behavior, but no implementation artifact conclusively proves 200% browser zoom layout, no clipping, no overlap, and no horizontal overflow at runtime. | Could be satisfied by CSS, but requires browser/manual or visual verification rather than static evidence. | Medium | Manual Review Required |
| A11Y-001 - Focus trap and restoration | Dialog focus mechanics exist, but source evidence showed corrupted Arabic/default accessibility labels/messages. | Dialog component, focus trap implementation, focus restoration, inert sibling handling, aria labels, keyboard handling. | `frontend/src/components/ui/Dialog.tsx`; `frontend/app/globals.css`; `frontend/src/features/lists/AddPlaceDialog.tsx`; `frontend/src/features/lists/CreateListDialog.tsx`; `frontend/src/features/places/CreatePlaceDialog.tsx`; `frontend/src/features/ratings/RatePlaceDialog.tsx`. | `Dialog`; focusable element query; Tab/Shift+Tab trap; Escape close; previous focus restoration; default close/status labels. | Focus behavior is implemented, but accessible text quality cannot be accepted when default labels/messages appear corrupted in source output. | Could be overridden by caller-provided labels, but default component accessibility remains risky. | High | Keep as Partial |
| A11Y-002 - Keyboard-operable rating control | Range slider exists, but the approved exact screen-reader contract is not fully matched. | Rating control component, range input attributes, keyboard-operable native input, aria label/value text, visual stars. | `frontend/src/components/ui/RatingControl.tsx`; `frontend/src/features/ratings/RatePlaceDialog.tsx`; `frontend/app/globals.css`. | `RatingControl`; `input type="range"`; `min=1`; `max=10`; `step=0.5`; `aria-valuetext`; rating value display. | Existing evidence proves keyboard-operable numeric control, but not the required announcement `Rating, X.X out of 10`; source output also indicated corrupted Arabic/star text risk. | Could be announced acceptably by the browser, but the documented exact accessible announcement is not explicitly implemented. | High | Keep as Partial |
| OPS-001 - `/api/v1` prefix | Version prefix behavior exists, but unsupported-version/error/logging governance is incomplete. | Backend router prefixes, unversioned health router, frontend path prefixing, double-prefix prevention, unsupported version handling, logs. | `backend/app/main.py`; `backend/app/api/auth.py`; `backend/app/api/places.py`; `backend/app/api/lists.py`; `backend/app/api/ratings.py`; `backend/app/api/profile.py`; `backend/app/api/health.py`; `frontend/src/lib/api.ts`. | `app.include_router(..., prefix="/api/v1")`; unversioned health router; `versionedPath`; product API routes. | Evidence proves versioned routing and client prefixing. It does not prove unsupported `/api/v9` structured error handling or safe version-prefix logging required by the stories. | Unsupported version may fall through framework 404 handling, but that is not the approved structured contract. | High | Keep as Partial |
| OPS-002 - `{data, meta}` collections | Envelope exists, but pagination defaults and invalid-pagination error behavior do not match approved decisions. | Collection response schemas, collection endpoint defaults, pagination validation, error handling. | `backend/app/core/schemas.py`; `backend/app/api/places.py`; `backend/app/api/lists.py`; `backend/app/core/errors.py`; `backend/app/main.py`. | `CollectionResponse`; `CollectionMeta`; `collection_response`; `GET /api/v1/places`; `GET /api/v1/lists`; `GET /api/v1/lists/public`. | Evidence proves `data` and `meta`, but endpoint defaults use `limit=100` in places/lists rather than approved default `20`; invalid pagination uses framework validation shape instead of approved error envelope. | Could be configured outside route defaults, but no runtime/config evidence was found. | High | Keep as Partial |
| OPS-003 - Structured error contract | Error helpers exist, but the implemented envelope differs from the approved contract and observability/audit pieces are incomplete. | Error helper, exception handlers, request ID middleware, API client error parsing, structured logging/audit/metrics searches. | `backend/app/core/errors.py`; `backend/app/main.py`; `backend/app/core/rate_limit.py`; `frontend/src/lib/api.ts`; `backend/app/api/auth.py`; `backend/app/api/lists.py`; `backend/app/api/places.py`; `backend/app/api/ratings.py`; `backend/app/api/profile.py`. | `api_error`; `http_exception_handler`; `validation_exception_handler`; `X-Request-ID` middleware; frontend `ApiError`; rate-limit handling. | Implementation uses FastAPI `detail` style rather than approved top-level `error` envelope with required `requestId`; structured JSON logs, metrics, alerts, and audit events were not found. | Some logging may exist via hosting/runtime, but no project implementation evidence was found. | High | Keep as Partial |
| OPS-004 - Backend liveness check | Liveness endpoint exists and is lightweight, but documented schema/observability requirements are incomplete. | Liveness endpoint, health router registration, no-auth behavior, dependency checks, schema fields, metrics/alerts/deployment marker. | `backend/app/api/health.py`; `backend/app/main.py`; `backend/app/core/config.py`; `backend/app/db/session.py`. | `GET /health/live`; `live`; unversioned health router. | Evidence proves endpoint and no database check. It does not prove `environment`, `timestamp`, `requestId` body fields, metrics, alerts, latency SLI, or deployment marker. | Some fields may be injected by middleware/header, but not in the documented liveness payload. | High | Keep as Partial |
| OPS-005 - Backend readiness check | Readiness endpoint exists and checks database connectivity, but migration/schema readiness contract is incomplete. | Readiness endpoint, database connectivity check, SQLAlchemy error path, Alembic files, schema compatibility/migration revision handling. | `backend/app/api/health.py`; `backend/app/main.py`; `backend/app/db/session.py`; `backend/migrations/env.py`; `backend/migrations/versions/20260618_0001_sprint_1_foundation.py`; `backend/migrations/versions/20260619_0002_sprint_2_ratings_visibility.py`; `backend/migrations/versions/20260620_0003_sprint_3_refresh_tokens.py`; `backend/migrations/versions/20260620_0004_normalized_place_names.py`; `backend/migrations/versions/20260620_0005_place_taxonomy.py`; `backend/migrations/versions/20260621_0006_decimal_ratings.py`; `backend/migrations/versions/20260624_0007_public_owner_display_names.py`; `backend/alembic.ini`. | `GET /health/ready`; `ready`; `SELECT 1`; `SQLAlchemyError`; Alembic migration scripts. | Evidence proves DB connectivity readiness. It does not prove expected Alembic revision validation, schema compatibility check, documented `checks.database.status` schema, timeout policy, metrics, alerts, or deployment gate. | Migration logic exists under Alembic, but not integrated into readiness evidence. | High | Keep as Partial |
| OPS-006 - Frontend health page/JSON | Frontend health routes exist, but documented schema and deployment evidence are incomplete. | Frontend `/health` page, `/api/health` route, health payload, no-auth page behavior, deployment/environment/timestamp fields. | `frontend/app/health/page.tsx`; `frontend/app/api/health/route.ts`; `frontend/app/layout.tsx`; `frontend/next.config.ts`. | `/health` page component; `GET /api/health`; JSON `status`/`service`. | Evidence proves health endpoints. It does not prove documented `environment`, `timestamp`, deployment marker/version, latency evidence, or release gate. | Deployment marker may exist in environment/build tooling, but no application source evidence was found. | High | Keep as Partial |
| OPS-007 - Alembic schema evolution | Alembic migrations exist, but production migration safety and evidence workflows are incomplete. | Migration config, migration versions, database metadata imports, readiness integration, backup/restore/release-gate implementation. | `backend/migrations/env.py`; `backend/migrations/versions/20260618_0001_sprint_1_foundation.py`; `backend/migrations/versions/20260619_0002_sprint_2_ratings_visibility.py`; `backend/migrations/versions/20260620_0003_sprint_3_refresh_tokens.py`; `backend/migrations/versions/20260620_0004_normalized_place_names.py`; `backend/migrations/versions/20260620_0005_place_taxonomy.py`; `backend/migrations/versions/20260621_0006_decimal_ratings.py`; `backend/migrations/versions/20260624_0007_public_owner_display_names.py`; `backend/alembic.ini`; `backend/app/db/base.py`; `backend/app/api/health.py`; `.github/workflows/ci.yml`; `docs/37-render-deployment-guide.md`; `docs/38-ci-cd-and-deploy-contract.md`. | Alembic `env.py`; migration revision files; SQLAlchemy model imports; readiness endpoint; CI workflow. | Evidence proves schema evolution mechanism. It does not prove backup evidence, restore validation, migration timeout policy, readiness revision gate, SLI metrics, rollback/forward-fix workflow, or release gates. | Some deployment controls may live outside the repo, but no implementation evidence was available in project source. | High | Keep as Partial |
| QA-001 - Auth lifecycle coverage | Unable to verify because the evidence rule excludes test files and test-case documents, which are the natural implementation artifacts for QA coverage suites. | QA artifacts, automation catalog, coverage matrix, regression/smoke docs, but not accepted as implementation evidence under the current audit rule. | `docs/user-stories/QA_AUTOMATION_CATALOG.md`; `docs/user-stories/QA_COVERAGE_MATRIX.md`; `docs/user-stories/QA_SMOKE_SUITE.md`; `docs/user-stories/QA_REGRESSION_SUITE.md`; automated test directories were intentionally not used as evidence. | QA suite `QA-001`; auth lifecycle coverage mapping references. | The only plausible evidence is QA documentation/test automation, explicitly excluded by the implementation audit instruction. | Yes. It may exist as QA suite documentation or automated tests rather than product runtime code. | Low | Manual Review Required |
| QA-002 - Places/lists API coverage | Unable to verify because QA coverage implementation cannot be proven without using excluded test files or QA artifacts as evidence. | QA artifacts, automation catalog, coverage matrix, regression/smoke docs, but not accepted as implementation evidence under the current audit rule. | `docs/user-stories/QA_AUTOMATION_CATALOG.md`; `docs/user-stories/QA_COVERAGE_MATRIX.md`; `docs/user-stories/QA_SMOKE_SUITE.md`; `docs/user-stories/QA_REGRESSION_SUITE.md`; automated test directories were intentionally not used as evidence. | QA suite `QA-002`; places/lists coverage mapping references. | Product source code cannot prove QA-suite execution or coverage. The relevant evidence type was disallowed. | Yes. It may exist as QA suite documentation or automated tests rather than product runtime code. | Low | Manual Review Required |
| QA-003 - Ratings/profile/public-list coverage | Unable to verify because QA coverage implementation cannot be proven without using excluded test files or QA artifacts as evidence. | QA artifacts, automation catalog, coverage matrix, regression/smoke docs, but not accepted as implementation evidence under the current audit rule. | `docs/user-stories/QA_AUTOMATION_CATALOG.md`; `docs/user-stories/QA_COVERAGE_MATRIX.md`; `docs/user-stories/QA_SMOKE_SUITE.md`; `docs/user-stories/QA_REGRESSION_SUITE.md`; automated test directories were intentionally not used as evidence. | QA suite `QA-003`; ratings/profile/public-list coverage mapping references. | Product runtime source cannot verify QA-suite coverage. The relevant artifacts are non-runtime QA evidence and were excluded. | Yes. It may exist as QA suite documentation or automated tests rather than product runtime code. | Low | Manual Review Required |
| QA-004 - App regression flow | Unable to verify because regression-suite implementation cannot be proven using runtime product code alone. | QA artifacts, automation catalog, coverage matrix, regression/smoke docs, but not accepted as implementation evidence under the current audit rule. | `docs/user-stories/QA_AUTOMATION_CATALOG.md`; `docs/user-stories/QA_COVERAGE_MATRIX.md`; `docs/user-stories/QA_SMOKE_SUITE.md`; `docs/user-stories/QA_REGRESSION_SUITE.md`; automated test directories were intentionally not used as evidence. | QA suite `QA-004`; app regression flow mapping references. | Runtime product code cannot prove regression-suite coverage or CI mapping. The relevant evidence type was disallowed. | Yes. It may exist as QA suite documentation, automated tests, or CI configuration. | Low | Manual Review Required |
| ADMIN-001 - Admin access control and audit foundation | No admin module implementation evidence found. | Admin route search, admin API router search, permissions/roles/audit model search, backend router registration search. | `backend/app/main.py`; `backend/app/api`; `backend/app/modules`; `frontend/app`; `frontend/src`; `docs/feature-map/GAP_RESOLUTION_PLAN.md`. | No `frontend/app/admin`; no admin API router; no admin permissions model/service; no admin audit model/service; no admin router registration. | Absence of runtime admin UI/API/security foundation prevents implementation classification. | Could be planned under future roadmap or external admin tooling; no in-repo implementation evidence found. | High | Keep as Not Implemented |
| ADMIN-002 - User lookup and account status review | No admin user-lookup/status-review implementation evidence found. | Admin user-management route/UI/model/service search, account status review behavior search. | `backend/app/main.py`; `backend/app/api`; `backend/app/modules/auth`; `backend/app/modules`; `frontend/app`; `frontend/src`; `docs/feature-map/GAP_RESOLUTION_PLAN.md`. | No admin user lookup endpoint; no admin user status page; no status review service; no admin router. | Auth user model exists, but admin review/lookup behavior is absent. | Could be external operational tooling, but no project source evidence found. | High | Keep as Not Implemented |
| ADMIN-003 - Public list moderation | No admin public-list moderation implementation evidence found. | Admin moderation route/UI/service search, public-list hide/restore moderation action search. | `backend/app/main.py`; `backend/app/api/lists.py`; `backend/app/modules/lists/services.py`; `frontend/app`; `frontend/src`; `docs/feature-map/GAP_RESOLUTION_PLAN.md`. | No admin list moderation endpoint; no admin moderation UI; no moderation audit service. | Public list visibility exists for owners, but admin moderation is a separate documented feature and was not found. | Could be conflated with owner visibility controls, but that is not admin moderation. | High | Keep as Not Implemented |
| ADMIN-004 - Place moderation and correction | No admin place-correction implementation evidence found. | Admin place moderation route/UI/service search, place edit/correction endpoint search. | `backend/app/main.py`; `backend/app/api/places.py`; `backend/app/modules/places/services.py`; `frontend/app/places`; `frontend/src/features/places`; `docs/feature-map/GAP_RESOLUTION_PLAN.md`. | No admin place moderation endpoint; no place update endpoint; no admin correction UI. | Place creation exists, but moderation/correction behavior is absent. | Could be future user-facing place editing, but no implementation was found under admin or user place flows. | High | Keep as Not Implemented |
| ADMIN-005 - Duplicate place resolution | No admin duplicate-resolution implementation evidence found. | Duplicate detection, merge endpoint, admin duplicate UI/service, place model constraints. | `backend/app/api/places.py`; `backend/app/modules/places/services.py`; `backend/app/modules/places/models.py`; `frontend/app`; `frontend/src/features/places`; `docs/feature-map/GAP_RESOLUTION_PLAN.md`. | Unique normalized-name constraint; no merge endpoint; no admin duplicate queue; no duplicate resolution service. | Duplicate prevention exists at creation time, but duplicate resolution/merge workflow is absent. | Could be partially mitigated by duplicate rejection, but not implemented as duplicate resolution. | High | Keep as Not Implemented |
| ADMIN-006 - Abuse and content review queue | No abuse report/review queue implementation evidence found. | Abuse/report model search, review queue route/UI/service search, admin moderation search. | `backend/app/main.py`; `backend/app/api`; `backend/app/modules`; `frontend/app`; `frontend/src`; `docs/feature-map/GAP_RESOLUTION_PLAN.md`. | No abuse report model; no report endpoint; no review queue UI; no admin abuse service. | No runtime artifact supports abuse report intake or admin review queue behavior. | Could be handled outside app process, but no in-repo evidence found. | High | Keep as Not Implemented |
| ADMIN-007 - Beta operational dashboard | No admin dashboard implementation evidence found. | Admin dashboard page/API search, operational summary route/service search, metrics/status UI search. | `backend/app/main.py`; `backend/app/api`; `backend/app/modules`; `frontend/app`; `frontend/src`; `frontend/app/health/page.tsx`; `frontend/app/api/health/route.ts`. | No `frontend/app/admin`; no dashboard route; no operational summary endpoint; no admin dashboard component. | Health pages exist, but a beta operational dashboard is absent. | Could be external observability dashboard, but no project source evidence found. | High | Keep as Not Implemented |
| ROAD-001 - Guest public-list browsing | Public-list behavior exists only for authenticated users; guest access is not implemented. | Public-list backend dependencies, public-list frontend session checks, auth guard behavior. | `backend/app/api/lists.py`; `backend/app/modules/lists/services.py`; `frontend/app/public-lists/page.tsx`; `frontend/app/public-lists/[id]/page.tsx`; `frontend/src/lib/api.ts`; `backend/app/modules/auth/dependencies.py`. | `GET /api/v1/lists/public`; `GET /api/v1/lists/public/{list_id}`; `CurrentUser` dependency; frontend session checks. | Evidence shows public-list endpoints are still protected by authentication, directly preventing guest browsing. | Could exist as roadmap/future scope, but not as runtime implementation. | High | Keep as Not Implemented |
| ROAD-002 - User-facing place editing | No user-facing place editing implementation evidence found. | Place update endpoint search, edit-place UI/dialog search, place correction flow search. | `backend/app/api/places.py`; `backend/app/modules/places/services.py`; `frontend/app/places`; `frontend/src/features/places`; `frontend/src/components/ui/PlaceCard.tsx`; `docs/feature-map/GAP_RESOLUTION_PLAN.md`. | `POST /api/v1/places`; `GET /api/v1/places`; `GET /api/v1/places/{place_id}`; `CreatePlaceDialog`; no `PATCH /api/v1/places/{place_id}`. | Create/view exist, but no update endpoint, edit dialog, correction request, or edit page was found. | Could be future admin correction under ADMIN-004, but no user-facing editing implementation exists. | High | Keep as Not Implemented |

## Validation

- Reviewed only non-Implemented classifications: 29
- Implemented features excluded: Yes
- Application code modified: No
- Test-case files used as implementation evidence: No
- Duplicate reviewed feature IDs: 0
- Recommended reclassification to Implemented: 0
